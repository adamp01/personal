# Adam Wood's Blog

## TODO:
* Add formatting style and format all files accordingly.
* Sort all imports according to style rules.
* Ensure consistent typing across all files.
* Make API return posts in a more obvious format, like [{source, frontmatter, slug}] rather than [{post: {data, slug, content}}].
* Serialise MDX content in utils/blog.ts rather than in getStaticProps in [slug].tsx
* Order CSS in a nicer manner.
* Create additional Head component, so we don't keep redefining icon, title etc.
* Add "Next Post"/"Previous Post" at bottom of [slug].tsx
