---
import ImageBase from "./ImageBase.astro";

interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<div class="hero-wrap">
  <ImageBase src={src} alt={alt} width={1600} height={900} class="hero-img" />
</div>
